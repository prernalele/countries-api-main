import React, { useEffect, useState } from "react";
import data from "./data.json";

const Card = ({ allData }) => {
  console.log("data", allData);

  return <div>Card</div>;
};

export default Card;
